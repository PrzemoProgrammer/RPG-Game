export const PLAY_SCENE = {
    SCENE:{},
    setScene:function(scene){
        this.SCENE = scene
    }
}

export const HUD_SCENE = {
    SCENE:{},
    setScene:function(scene){
        this.SCENE = scene
    }
}

export const WINDOW_SCENE = {
    SCENE:{},
    setScene:function(scene){
        this.SCENE = scene
    }
}